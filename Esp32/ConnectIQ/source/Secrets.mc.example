// Copiaza acest fisier in Secrets.mc (langa el, in acelasi folder) si pune
// codul real — trebuie sa fie identic cu APP_SECRET din secrets.h (ESP32).
// Secrets.mc e ignorat de git, Secrets.mc.example nu.
const APP_SECRET = [0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00]b;
