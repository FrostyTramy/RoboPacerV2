#pragma once

// Copiaza acest fisier in secrets.h (langa el, in acelasi folder) si pune
// codul real — trebuie sa fie identic cu APP_SECRET din App.mc (Garmin).
// secrets.h e ignorat de git, secrets.h.example nu.
const uint8_t APP_SECRET[]    = { 0x00, 0x00, 0x00, 0x00, 0x00, 0x00 };
const uint8_t APP_SECRET_LEN  = 6;
